`import { countText } from '../../../helpers/count-text'`
`import { module, test } from 'qunit'`

module 'CountTextHelper'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = countText 42
  assert.ok result
