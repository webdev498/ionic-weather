import { noteq } from 'sln-mobile-ember/helpers/noteq';
import { module, test } from 'qunit';

module('Unit | Helper | noteq');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = noteq([42]);
  assert.ok(result);
});
