import { textOrSpace } from 'sln-mobile-ember/helpers/text-or-space';
import { module, test } from 'qunit';

module('Unit | Helper | text or space');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = textOrSpace([42]);
  assert.ok(result);
});
