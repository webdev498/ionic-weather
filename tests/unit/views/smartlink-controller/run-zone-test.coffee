`import { test, moduleFor } from 'ember-qunit'`

moduleFor 'view:smartlink-controller/run-zone'

# Replace this with your real tests.
test 'it exists', (assert) ->
  view = @subject()
  assert.ok view
