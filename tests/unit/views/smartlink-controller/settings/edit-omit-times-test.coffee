`import { test, moduleFor } from 'ember-qunit'`

moduleFor 'view:smartlink-controller/settings/edit-omit-times'

# Replace this with your real tests.
test 'it exists', (assert) ->
  view = @subject()
  assert.ok view
